version: 2
sphinx:
  configuration: docs/conf.py
build:
  os: ubuntu-22.04
  tools:
    python: "3.10"
  jobs:
    pre_build:
      - python scripts/document_rules.py
python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
